# Set xfce environment and _VIRTUALGL
<%= set_xfce %>

# Start ImageJ
<% if context.respond_to?(:input) && !context.input.to_s.empty? %>
/opt/ImageJ/ImageJ "<%= context.input %>" &
<% else %>
/opt/ImageJ/ImageJ &
<% end %>

# Start up xfce desktop (block until user logs out of desktop)
xfce4-session
